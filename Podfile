# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FirebaseLoginApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FirebaseLoginApp
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'PKHUD', '~> 5.0'

  target 'FirebaseLoginAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FirebaseLoginAppUITests' do
    # Pods for testing
  end

end
